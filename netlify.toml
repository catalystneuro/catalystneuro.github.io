[build]
  command = "npm run build"
  publish = "dist"

# Astro 7 requires Node 20 or newer, and this project's package.json asks for
# 22.12. Nothing in the repository previously pinned a version, so the build
# inherited whatever the Netlify project was configured with, which was older.
[build.environment]
  NODE_VERSION = "22"

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# The ASAP funded-project page was published under a slug derived from the
# program's full name. The title, and therefore the slug, is back to
# "ASAP NWB Adoption", so keep the old address working.
[[redirects]]
  from = "/funded-projects/aligning-science-across-parkinson-s"
  to = "/funded-projects/asap-nwb-adoption"
  status = 301

# The Pareto frontier post was retitled "What Happens When the Cost of
# Intelligence Drops 100x" and its file renamed to match. Keep the address it
# was first published under working.
[[redirects]]
  from = "/blog/pareto-frontier-cost-per-task"
  to = "/blog/cost-of-intelligence-drops-100x"
  status = 301

# Astro emits real static pages for every route, so no SPA catch-all is needed.
# A 404.html is generated automatically for unmatched paths.
